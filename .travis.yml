sudo: required
dist: trusty
before_install:
install:
before_script:
script:
  - ./package.sh ${DISTRIBUTION} ${ARCHITECTURE}
after_failure:
after_success:
env:
  matrix:
    - DISTRIBUTION=trusty
    - DISTRIBUTION=xenial
    - ARCHITECTURE=i386
    - ARCHITECTURE=amd64
