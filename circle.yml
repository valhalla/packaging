checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - sudo apt-get update

test:
  override:
    - ./package.sh

general:
  artifacts:
    - ./*_trusty
    - ./*_xenial
