sudo: required
dist: trusty
before_install:
install:
before_script:
script:
  - ./package.sh ${DISTRIBUTION} ${ARCHITECTURE}
after_failure:
after_success:
env:
  matrix:
    - DISTRIBUTION=trusty ARCHITECTURE=i386
    - DISTRIBUTION=xenial ARCHITECTURE=i386
    - DISTRIBUTION=trusty ARCHITECTURE=amd64
    - DISTRIBUTION=xenial ARCHITECTURE=amd64
